---
export const prerender = false;

import Layout from '../layouts/AdminLayout.astro';

const accessToken = Astro.cookies.get('sb-access-token');
const refreshToken = Astro.cookies.get('sb-refresh-token');

if (accessToken && refreshToken) {
	return Astro.redirect('/admin');
}

const error = Astro.url.searchParams.get('error');
const errorMessages: Record<string, string> = {
	missing_fields: 'Ingresa tu correo y contraseña.',
	invalid_credentials: 'Correo o contraseña incorrectos.',
};
---

<Layout title="Iniciar sesión" nav={false}>
	<main class="login">
		<form action="/api/auth/signin" method="post">
			<h1>Iniciar sesión</h1>
			{error && <p class="error">{errorMessages[error] ?? 'Ocurrió un error, intenta de nuevo.'}</p>}
			<label for="email">Correo</label>
			<input type="email" name="email" id="email" required autocomplete="email" />
			<label for="password">Contraseña</label>
			<input type="password" name="password" id="password" required autocomplete="current-password" />
			<button type="submit">Entrar</button>
		</form>
	</main>
</Layout>

<style>
	.login {
		display: flex;
		/* dvh, because 100vh on iOS Safari counts the address bar and pushes a
		   vertically centred form partly under it. */
		min-height: 100vh;
		min-height: 100dvh;
		align-items: center;
		justify-content: center;
		padding: 1.5rem 1rem;
	}
	form {
		display: flex;
		/* A fixed 320px form has its fields touching both edges on a 320px
		   phone. Cap it instead, and let the wrapper's padding hold the margin. */
		width: 100%;
		max-width: 320px;
		flex-direction: column;
		gap: 0.75rem;
	}
	.error {
		color: #b91c1c;
		margin: 0;
	}
	input {
		/* 1rem exactly: iOS Safari zooms the whole page in when a focused
		   field's text is smaller than 16px. */
		font-size: 1rem;
		padding: 0.5rem;
		border: 1px solid #ccc;
		border-radius: 4px;
	}
	button {
		padding: 0.6rem;
		border: none;
		border-radius: 4px;
		background: #111827;
		color: white;
		cursor: pointer;
	}
</style>
